import Stylesheet from "reactjs-stylesheet";

export const styles = Stylesheet.create({
  container: {
    display: "flex",
    flex: 0.2,
  },
  header: {
    alignSelf: "center",
    position: "fixed",
    top: 0,
  },
});
